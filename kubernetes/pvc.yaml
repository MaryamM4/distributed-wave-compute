apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: schrodinger-pvc
  namespace: schrodinger
spec:
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 5Gi
  storageClassName: efs-sc